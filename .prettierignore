node_modules
dist
.next
.turbo
coverage
pnpm-lock.yaml
# Markdown: excluded deliberately. Prettier reformats code inside fenced
# blocks, and the ADRs, the runbook and doffin-api-findings.md quote real API
# responses as evidence — reformatting them edits the evidence. `md` is also
# out of the format globs in package.json, so the two configs agree.
*.md

# Regenerated by `next typegen` on every build, with formatting we do not control.
next-env.d.ts

# Owned and rewritten by drizzle-kit on every `db:generate`.
packages/db/drizzle/meta
